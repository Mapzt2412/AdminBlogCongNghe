import MainLayout from "../MainLayout/MainLayout.js"

const Home = () => {
    return <>
        <MainLayout leftMenu={'leftMenu'} contentHeader={'contentHeader'} content={'content'} />
    </>
} 

export default (Home)